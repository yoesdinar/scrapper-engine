# Docker Image Configuration
# These variables are automatically set by GitHub Actions during deployment
# For manual deployment, update these values

IMAGE_TAG=latest
REGISTRY=ghcr.io
IMAGE_PREFIX=your-username/your-repo-name
